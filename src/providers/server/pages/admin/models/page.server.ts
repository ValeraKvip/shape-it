import { ModelManager } from "../../../../../Schema";
import ServerProvider from "../../../ServerProvider";

export function get_admin_models_page_server(serverProvider: ServerProvider, model: ModelManager){
    return ``;
}